/**
 * Regisztrációhoz szükséges MW, adatbázisba elmenti az új user-t
 * Fejlesztés alatt
 */

module.exports = function(objectrepository) {
    return function(req, res, next){
        return next();
    };
};