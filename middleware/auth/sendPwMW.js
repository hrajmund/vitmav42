/**
 * Kiírja a jelszót a konzolra, ha elfelejtette a user
 * Fejlesztés alatt
 */

module.exports = function(objectrepository) {
    return function(req, res, next){
        return next();
    };
};