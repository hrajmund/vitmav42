/*function login_submit() {
    const email = document.getElementById("login_email");
    const pass = document.getElementById("logint_password");
    const btn = document.getElementByClass("btnlog");

    btn.addEventListener("click", (e) => {
        if(email == "" && pass == ""){console.log("haha");}
        e.preventDefault();
        const username = email.username.value;
        const password = pass.password.value;
        if(username == "root@xyz.com" && password == "1234"){
            window.alert("yes");
            location.reload();
        }else{
            alert("Wrong!");
        }
    }
    );

}
*/